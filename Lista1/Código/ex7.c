#include <stdio.h>
#include <stdlib.h>

int main()
{
  float factoryPrice, distributorTaxes, generalTaxes;
  int opt;

  while (opt != 2) {
    if (opt == 1) {
      system("cls");

      printf("Enter the factory price of the vehicle: $");
      scanf("%f", &factoryPrice);
    }
  }

  return 0;
}
